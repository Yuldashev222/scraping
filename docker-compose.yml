services:
  app:
    build: .
    environment:
      - ENV=development
    volumes:
      - ./src:/app
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
